use make to compile.

Reads inp.qasm by default, can be given another input file name as parameter.

while running use commands to print info about lines [beg, end)
p beg end
abs beg end
ang beg end
use command q to quit.

Empty lines are curently skipped in counting line numbers.
